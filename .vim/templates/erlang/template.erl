#!/usr/bin/env escript

% module name (file name without extensiona nd no dashes)
-module(template).

% while developing expose all functions
-compile(export_all).

%% @doc Entry point into the program.

main() -> 
    done.
